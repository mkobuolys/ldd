name: ldd
description: "Fluttercon 2024 - Cat-like Development: Flutter for Efficiency"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.3 <4.0.0"

dependencies:
  device_preview: ^1.2.0
  flutter:
    sdk: flutter
  flutter_deck: ^0.14.0
  rive: ^0.13.7
  gskinner_example:
    path: packages/gskinner_example

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/rive/
